export type NavItem = {
  label: string
  href: string
}

export type StatItem = {
  label: string
  value: string
}

export type FeatureItem = {
  title: string
  description: string
  highlight: string
}

export type CommandCategory = {
  category: string
  commands: string[]
}

export type Testimonial = {
  quote: string
  author: string
  role: string
}
